version: '3.8'

services:
  tiktokcoins-payool:
    build: .
    container_name: tiktokcoins_payool_app
    restart: unless-stopped
    ports:
      - "8085:80"
    networks:
      - payool-network
    environment:
      - NODE_ENV=production

networks:
  payool-network:
    external: true
