import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.payool.tiktokcoin',
  appName: 'TikTok Coins by PayOol',
  webDir: 'dist'
};

export default config;
